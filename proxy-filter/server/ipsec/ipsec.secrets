# Tells charon (the IKE daemon) where the server's private key lives.
# Path is relative to /opt/homebrew/etc/ipsec.d/private/ on macOS, or
# /etc/ipsec.d/private/ on Linux. setup-server-mac.sh symlinks our
# vpn-server.key into the right place.

: RSA vpn-server.key
